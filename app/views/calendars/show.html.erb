<div class="container flex-1 p-8 overflow-y-auto max-w-7xl mx-auto">

  <h1 class="text-2xl font-bold text-slate-800 py-8">Meetings</h1>
  <%= month_calendar(events: @meetings) do |date, meetings| %>
    <%= date %>
  <% end %>
</div>
