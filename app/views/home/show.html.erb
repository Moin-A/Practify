<%# Dashboard Main Container %>
<div class="mx-auto" data-controller="home">
  <%= render HomePage::HomeComponent.new(
    current_user: @current_user,
    selected_slot_id: @selected_slot_id,
  ) %>
</div>
