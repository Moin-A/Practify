<%= stylesheet_link_tag "vonage_session" %>
<%= render VonageSessionComponent.new(
  call_room: @call_room,
  token: @token,
  app_id: @app_id,
  current_user: current_user,
  appointment_id: @appointment.id
) %>